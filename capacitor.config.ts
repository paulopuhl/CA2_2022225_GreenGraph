import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CA2_2022225_GreenGraph',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
